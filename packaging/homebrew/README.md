# Homebrew packaging

`matrixsh.rb` is a template formula.
To publish:
- Create a tap repository (e.g. `yourorg/homebrew-tap`)
- Put the formula under `Formula/matrixsh.rb`
- Update `url` and `sha256` to your release tarball

